import React from "react"
import { CartDash } from 'react-bootstrap-icons';

const CartWidget = () => {
    return (
        <>
            <CartDash color="white" size={40} className="ml-5 mt-2 pt-1"/>
        </>
    )
}

export default CartWidget